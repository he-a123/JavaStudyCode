package day07;

/*import java.util.ArrayList;
import java.util.List;

public class ArraylistStudent {
    /*定义一个学生类Student，包含三个属性姓名、年龄、性别，创建三个学生对象存入ArrayList集合中。
  A：遍历集合遍历输出。
  B：求出年龄最大的学生，让后将该对象的姓名变为：葫芦娃。*/
    /*public static void main(String[] args) {
        ArrayList<Student> array = new ArrayList<Student>();
        addArrayist(array);
        PintlnArrayList(array);
        maxAge(array);

    }
    public static void addArrayist(ArrayList<Student> array){
        Student st1 = new Student();
        Student st2 = new Student();
        Student st3 = new Student();
        st1.name = "寒冰";
        st1.age = 18;
        st1.sex = '女';
        st2.name = "小炮";
        st2.age = 58;
        st2.sex = '女';
        st3.name = "诺手";
        st3.age = 48;
        st3.sex = '男';
        array.add(st1);
        array.add(st2);
        array.add(st3);

    }
    public static void PintlnArrayList(ArrayList<Student>array){
        for (int i = 0; i < array.size(); i++) {
            System.out.println("姓名：" + array.get(i).name  +"  性别 ："  +
                    array.get(i).sex  + "  年龄：" + array.get(i).age);

        }
    }
    public static void maxAge(ArrayList<Student> array){

       /* int a = array.get(0).age;
        int b = array.get(1).age;
        int c = array.get(2).age;
        int d = a > b ? a : b;
        int max = d > c ? d : c;
        System.out.println(array.get(max));*/
     /*  int max = array.get(0).age;
        for (int a = 0; a < array.size(); a++) {
            if(array.get(a).age > max){
                max = array.get(a).age;
            }
            //System.out.println(array.get(a).name + max + array.get(a).sex);
        }
        for (Student s: array){
            if(s.age == max){
            s.name = "葫芦娃";
            }
        }
        System.out.println();
        for(Student s : array) {
            System.out.println("姓名：" + s.name+ " 性别："+s.sex +" 年龄："+ s.age);
        }
    }


}*/
